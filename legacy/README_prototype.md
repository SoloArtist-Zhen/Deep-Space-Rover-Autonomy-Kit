# Legacy Prototype

The original single-file research prototype has been preserved under `legacy/prototype/`.

- `baseline_main.py` keeps the original benchmark pipeline.
- `modules/` keeps the initial terrain, planning, control, multi-agent, and visualization code.

This folder is retained for baseline comparison and reproducibility, while the main repository now targets a hierarchical ROS2-oriented autonomy stack.
